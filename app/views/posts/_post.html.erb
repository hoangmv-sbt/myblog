<div id="<%= dom_id post %>">
  <p>
    <strong>Title:</strong>
    <%= post.title %>
  </p>

  <%= image_tag post.image if post.image.attached? %>

  <p>
    <strong>Content:</strong>
    <%= raw post.content %>
  </p>
</div>
